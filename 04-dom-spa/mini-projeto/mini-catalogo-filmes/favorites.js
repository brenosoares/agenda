const KEY = 'catalogo_favoritos'; export function getFavoritos() { try { return JSON.parse(localStorage.getItem(KEY)) || []; } catch { return []; } } export function toggleFavorito(id) { const favs = new Set(getFavoritos()); favs.has(id) ? favs.delete(id) : favs.add(id); localStorage.setItem(KEY, JSON.stringify([...favs])); return [...favs]; } export function isFavorito(id) { return getFavoritos().includes(id); }