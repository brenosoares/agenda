const API_URL = 'https://www.omdbapi.com/'; const API_KEY = 'demo'; export async function buscarFilmes(query, page = 1) { if (!query) return { Search: [], totalResults: 0 }; const url = new URL(API_URL); url.searchParams.set('apikey', API_KEY); url.searchParams.set('s', query); url.searchParams.set('page', page); const res = await fetch(url); const json = await res.json(); if (json.Response === 'False') { return { Search: [], totalResults: 0, Error: json.Error }; } return json; } export async function detalhesFilme(id) { const url = new URL(API_URL); url.searchParams.set('apikey', API_KEY); url.searchParams.set('i', id); const res = await fetch(url); return res.json(); }