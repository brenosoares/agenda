# E2 – Galeria de Imagens Responsiva

Usar `<figure>`, `<figcaption>` e `<picture>` com fontes em WebP + fallback.

Critérios:
- `alt` descritivo
- `figcaption` contextual
- Responsividade sem distorção

Extensão: `loading="lazy"`.