# E3 – Formulário de Cadastro Acessível

Campos: nome, email, senha, confirmar senha, newsletter (checkbox).

Critérios:
- `label for` correto
- `fieldset` + `legend`
- Validação nativa (`required`, `type`)
- Mensagens claras (placeholder não substitui label)